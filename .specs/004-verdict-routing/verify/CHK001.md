# CHK001 — a real day sorted by Verdict (2026-09-23)

Run by Mr Claude on the owner's standing /goal approval. Owner tick (`flow tick CHK001 --by user`)
is left to the owner.

## Setup
- Binary: `cargo build --release -p worklog-cli` at 737d516 (+ ticks). Branch daemon on :9323.
- `worklog verdict serve` → `uv run --with git+https://github.com/Heman10x-NGU/Verdict-open-jev@30f1556… --with huggingface_hub`.
  - First run: model files downloaded to `~/.local/share/worklog/verdict-model` (1.1 GB) in 36 s.
  - Later start (model present): healthy in **6 s** (≤ 10 s ✅).
- DB backed up first: `~/.local/share/worklog/worklog-pre-004.db`.

## First real run (before FR-10) — found two defects
- Daemon froze: GET/POST /settings read the keychain on async workers; a keychain permission
  dialog for the new binary blocked every worker. Fixed in 1f8013d (spawn_blocking).
- Filed 3 right + **1 wrong** ("The Morning Checkup", claude.ai → claude-3p-config). Ratios could
  not separate it (right ×1.06–×1.23 vs abstain, wrong ×1.16). → amendment FR-10 + default ×1.20 (T007).

## Final run (737d516)
`POST /infer {"day":"2026-09-23"}` → `{"blocks":14,"minutes":312}` in **8.6 s** for 27 events
(~23 model calls → ~0.37 s/event, ≤ 0.5 s ✅).

| event | filed to | origin | right? |
|---|---|---|---|
| team-talos: github.com/aproorg/vitinn-infra/pull/802 | vitinn-infra | rule | ✅ |
| team-talos: github.com/aproorg/vitinn-infra/pull/811 | vitinn-infra | rule | ✅ |
| DM: github.com/aproorg/vis-document-analyzer/pull/63 | vis-document-analyzer | rule | ✅ |
| DM: `cd ~/Desktop/Work/vitinn-infra` | vitinn-infra | rule | ✅ |
| 23 others (DM small talk, news, Graphify, Rabbit, Slack API, Morning Checkup, legal site) | Unsorted | — | ✅ |

**4 right, 0 wrong, 0 filed by the model.** Screenshot: `CHK001-day-page.jpeg` (Unsorted 23,
the 4 rule rows with folder + `rule` tag, 14 blocks).

## Owner result
Owner (YYYY-MM-DD): day page looks right ? · 0 wrong filings ?
