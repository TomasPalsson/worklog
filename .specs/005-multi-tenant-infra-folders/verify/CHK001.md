# CHK001 — evidence

Checked on a **sandbox** worklog home (fresh `worklog db migrate`, schema v13,
made-up blocks). Copying the real DB into the sandbox was refused by the
permission classifier, so the real 2026-09-24 re-run is still the owner's.

## Seen in the sandbox (2026-09-25, new daemon + `bun --bun next dev`)

| Check | Result | Screenshot |
|---|---|---|
| Seed flags `vitinn-infra` and `genai-infra` as many-customers | both ticked in Folder mappings | `billing-registry-desktop.png` |
| Tenants discovered from `~/Desktop/Work`, hidden dirs skipped | 25 unmatched + 19 matched listed, no `.terraform` | `billing-registry-desktop.png` |
| Link a tenant (`builds` → Not a customer) | saves on change, "Saved", moves to Matched (15→14) | — (DOM check) |
| Block with Sjúkra branch clue then MMS reflog clue | card shows `MMS 69% · Sjúkra 31% auto` | `day-blocks-desktop.png` |
| Block with no clue | card shows `APRÓ guess` (folder pin), not "Unresolved" | `day-blocks-desktop.png` |
| Split evenly → Save | card shows `MMS 50% · Sjúkra 50% set by you`; row in `block_customer_shares` | `split-editor-mobile.png` |
| 400 px width | editor stacks, no overflow from new UI (day-nav overflow is pre-existing) | `split-editor-mobile.png` |

## Still for the owner (real data)

1. `worklog export 2026-09-24` → a Sjúkra line for blocks 4098, 4101, 4102 (~2.5 h) and an APRÓ line for the rest of vitinn-infra.
2. Change one block's split in the review UI, re-estimate the day, the split is still there.
