Discovered: db.rs tests moved to db_test.rs (size-guard, 887 > 400 lines); seed gated to the v14 upgrade so a deleted seeded deild stays deleted — fold into T001
Discovered: T002 duplicated billing_registry::alias_matches as a private keyword_matches (file ownership) — fold into T003 (expose alias_matches, delete the copy)
Ruling: size-guard on pre-existing oversized files (daemon.rs 5368, billing.rs 1687, billing_registry.rs 752, estimate.rs 2763, infer.rs 1357, block_service.rs 893 lines; routing.rs 399→408) — new code goes in new sibling files (#[path] modules like daemon_tenants.rs), big files only get wiring lines; splitting them is out of scope — cost if wrong: files stay big
Ruling: folder-default deild applies when slice customer == resolved.customer (pin OR alias-matched), not strictly the pinned customer (FR-03 wording) — parked — pre-existing behaviour from before spec 006, only hits a folder pinned with Verkefni but no customer — cost if wrong: an alias-matched customer on such a folder gets that folder's Verkefni
Ruling: branch review dropped (blind re-score < 80) — F7 split-editor total uses === 100, ignores ±0.1 tolerance (75); F4 unseen cursor 0 on an empty change table suppresses the first live pop-up, catch-up still shows it (70); F6 v1 load_shares path gives 0 slices only on a corrupt rows_json + dead v1 helpers (70); F8 move_line_deild not transactional, no is_personal check (70); F10 line-header customer pin edits only the first folder of a multi-folder line (70); F14 pre-v14 days have no snapshot baseline so their first automatic change is not logged (70); F15 unused constants/toast param (30); F11 refresh glue copies (25); F12 task-id comments (25); F13 no list-size caps, local only (10) — cost if wrong: minor UX/robustness gaps, no invoice data invented
