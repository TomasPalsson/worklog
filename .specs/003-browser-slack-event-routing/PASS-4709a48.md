# PASS — 4709a48 (2026-09-23)

Range: 0fa7830..4709a48

## G001 project gates
`flow check --fix` → exit 1, "No supported project file found" (no root manifest; code lives in
`rust/` and `web/`). Ran the project's own commands from CLAUDE.md instead:

| Command | Exit | Result |
|---|---|---|
| `cargo fmt --manifest-path rust/Cargo.toml --all -- --check` | 0 | clean |
| `cargo clippy --manifest-path rust/Cargo.toml --all-targets --all-features -- -D warnings` | 0 | clean |
| `cargo test --manifest-path rust/Cargo.toml` | 0 | 599 passed, 0 failed |
| `cd web && bun test` | 0 | 113 pass, 0 fail |
| `cd web && bun run typecheck` | 0 | clean |
| `cd web && bun run build` | 0 | built |

Rust ran at 830c4a7; 830c4a7..4709a48 touches only `web/app/globals.css` and `.specs/`.
Web commands re-ran at 4709a48.

## G002 branch review
`review-package 0fa7830 HEAD` → workflow `flow:review-diff` (run wf_e69d8aae-089), lenses
correctness, security, gaming, cross-file, slop; blind 0–100 re-score, keep ≥ 80.
- 16 agents, 0 errors, 0 failed lenses.
- 1 kept (score 88, cross-file): `.routed-row` had no CSS, so labelled rows hit the same
  22px-column bug CHK003 found. Fixed in 4709a48 (`.routed-row` shares the unsorted-row rules).
  Checked in the browser: tag 72px, main/folder/badge all in column 2 (x=377, w=826);
  verify/G002-routed-row-layout.png.
- 9 dropped (< 80).

## Converge
All 16 T/CHK done; NOTES `Discovered:` lines are all `defer`. No new tasks. No-op.

## G003 verification evidence
`verify/` holds CHK001.md, CHK001-web-ext-lint.txt, CHK002.md, CHK003.md,
CHK003-unsorted-layout.png, G002-routed-row-layout.png.
