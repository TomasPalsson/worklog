# CHK003 — day page with real data (after T013 + T014)

Pre-check (Mr Claude, 2026-09-23 15:40Z):
- Old branch binary was built 14:44, before T014 (15:25) and T013 (15:32). Rebuilt:
  `cargo build --release -p worklog-cli` → exit 0. Branch daemon restarted on 9323.
- Preflight (T013): `OPTIONS /browser/heartbeat` with `Origin: moz-extension://…`,
  `Access-Control-Request-Headers: content-type` → `HTTP/1.1 200 OK`,
  `access-control-allow-origin: moz-extension://…`, `allow-methods: POST`,
  `allow-headers: content-type`. (Was 405 in CHK002.)
- DM names (T014): `worklog collect slack --days 3` → `events=64 errors=0`.
  `/days/2026-09-23/routed` → 13 slack events. Titles: "Benedikt Óskarsson",
  "Pall Jonsson", "Sindri", "Jón Levy", "Hera Brá Tómasdóttir", "team-talos".
  No raw `U…` ids.
- Firefox: 0 events so far, `last_heartbeat: null`. The add-on sends only while a
  Firefox window is focused and not idle; it needs the user at the keyboard.
- Tags: none yet (`label_origin: null` on all). No rules exist and
  `laya_reachable: false`, so no rule/guess tags are possible; a `fix` tag appears
  once the user sorts an event.
- Branch web on http://localhost:3334/2026-09-23 → 200.

Human steps:
1. Use Firefox for a minute or two (window in front). Check
   `curl -s http://127.0.0.1:9323/routing/status` shows `last_heartbeat` set.
2. Open http://localhost:3334/2026-09-23 (reload).
3. Check: Firefox events show a container, Slack events show a channel or a DM name.
4. Sort one unsorted event → it leaves the unsorted list, shows in a block, tagged `fix`.
5. Undo when done: stop the branch daemon + web, then
   `launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/is.p5.worklog.daemon.plist`

Result (user, 2026-09-23, screenshot of Unsorted (15)):
- DM names shown: YES ("Benedikt Óskarsson", "Pall Jonsson", "Sindri", "Jón Levy").
- Channel shown: YES ("team-talos").
- Firefox: heartbeats now stored — `last_heartbeat: 2026-09-23T15:45:16Z`, 2 firefox events
  ("Slack API: Applications | APRÓ - Slack", container null = default container). Not seen on screen yet.
- Layout: BROKEN. "always for this channel" wraps one word per line under the Slack tag.
  Cause: `.event-row` is `grid-template-columns: 22px 1fr` (web/app/globals.css:1373);
  UnsortedList renders 4 children (SourceTag, body, label.unsorted-always, PalettePicker), so
  the label lands in the 22px column. No CSS exists for `.unsorted-row` / `.unsorted-always`.
- Sort one event → block: not tried yet.
- NOT PASSED yet.

Layout fix (Mr Claude, user said fix directly, no spec task):
- `web/app/globals.css`: `.unsorted-row` grid `72px 1fr`, non-tag children in column 2,
  tag width 100%, `.unsorted-always` inline-flex.
- Screenshot after: verify/CHK003-unsorted-layout.png — checkbox on one line, tag column aligned
  for Slack and Firefox rows.
- `bun test` → 113 pass, 0 fail. `bun run typecheck` → exit 0.
- Still for the user: sort one event → it moves into a block.
