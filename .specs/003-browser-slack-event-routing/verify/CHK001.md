# CHK001 — signed add-on installs in normal Firefox and survives a restart

Pre-check (Mr Claude, 2026-09-23): `bunx web-ext lint --source-dir extension/firefox`
→ exit 0, 0 errors, 1 warning (MISSING_DATA_COLLECTION_PERMISSIONS). Full output: CHK001-web-ext-lint.txt

Human steps:
1. cd extension/firefox
2. bunx web-ext sign --channel unlisted --api-key "$AMO_JWT_ISSUER" --api-secret "$AMO_JWT_SECRET"
3. Firefox → about:addons → gear → Install Add-on From File… → pick the .xpi in web-ext-artifacts/
4. Quit Firefox fully, reopen, about:addons → add-on is still listed and enabled

Result (user, 2026-09-23): signed yes · installed yes · enabled after restart yes
