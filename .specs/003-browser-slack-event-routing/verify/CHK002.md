# CHK002 — day page with real data

Pre-check (Mr Claude, 2026-09-23):
- Live daemon is the installed `~/.local/bin/worklog` 0.11.2. `GET /routing/status` and
  `GET /days/2026-09-23/routed` → 404, so it cannot serve this page.
- Branch binary built: `cargo build --release -p worklog-cli` → exit 0,
  `rust/target/release/worklog` (0.11.2, branch code).
- Branch daemon migrates the schema on start. DB: `~/.local/share/worklog/worklog.db`.

Human steps (from the worktree root):
1. Back up: `sqlite3 ~/.local/share/worklog/worklog.db ".backup /tmp/worklog-pre-003.db"`
2. Stop the live daemon: `launchctl bootout gui/$(id -u)/is.p5.worklog.daemon`
3. Run the branch daemon: `rust/target/release/worklog daemon`  (leave it running)
4. Other terminal: `cd web && WORKLOG_DAEMON_URL=http://127.0.0.1:9323 bun run dev`
5. Open http://localhost:3333, go to today (or a day with Firefox + Slack activity).
6. Check: Firefox events show container, Slack events show channel, each has a rule/fix/guess tag.
7. Pick one unsorted event, give it a label → it leaves the unsorted list and shows in a block.
8. Undo: Ctrl-C both, `launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/is.p5.worklog.daemon.plist`

Run by Mr Claude (2026-09-23, user delegated the check):
- DB backed up to `~/.local/share/worklog/worklog-pre-003.db` (168 MB).
- Installed daemon booted out; branch daemon running on 9323 → `/routing/status` 200:
  `{"last_heartbeat":null,"last_slack":null,"laya_reachable":false}`.
- Branch web on :3334 (3333 held by the installed web UI) → `GET /` 307 → `/2026-09-23` 200, UnsortedList rendered.
- `/days/2026-09-23/routed` → `[]`. Events in DB by source: claude, github_commit, github_pr only — 0 browser, 0 slack.
- Slack: `worklog collect slack --days 3` → skipped, missing secret `slack_user_token`.
- Firefox: add-on only sends while the Firefox window is focused and not idle; no heartbeat arrived.
- NOT PASSED: no real Firefox/Slack data to look at. Did not inject fake events into the real DB.

Result (user, YYYY-MM-DD): container/channel shown ? · tags shown ? · sorted event moved into block ?

Follow-up (Mr Claude, 2026-09-23 15:05Z):
- Slack token set by user → `worklog collect slack --days 3` → `events=59 errors=0`; `/routing/status` last_slack=2026-09-23T14:02:18Z.
- Add-on installed by user; still 0 firefox events. Swapped daemon for a one-shot listener; Firefox sent:
  `OPTIONS /browser/heartbeat` · `Origin: moz-extension://2de737c4-…` · `Access-Control-Request-Method: POST` · `Access-Control-Request-Headers: content-type` · `Sec-Fetch-Mode: cors`
- Same preflight to the branch daemon → `HTTP/1.1 405 Method Not Allowed`, `allow: POST`. The POST never follows.
- FAILED: Firefox half blocked by the missing preflight handler. Slack half has data.
- `/days/2026-09-23/routed` → 8 slack events. Sample: `title: "U06815WTGSE"` (DM shows a raw user id, not a name), `container: null`, `label_origin: null`, `label_confidence: null`. No rule/fix/guess tag on any; `laya_reachable: false`, so no guesses can be made.
